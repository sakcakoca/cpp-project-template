# ─────────────────────────────────────────────────────────────────────────────
# GitHub Actions CI — full cross-platform build & test matrix
#
# Stages:
#   1. test          — plain tests (no sanitizers, all platforms)
#   2. valgrind      — plain debug builds + ctest memcheck (Linux only)
#   3. leaks         — plain debug builds + leaks check (macOS only)
#   4. sanitizers    — sanitizer-enabled tests (platform-dependent)
#   5. static-analysis — PVS-Studio + cppcheck + Sonar C++ (Linux only)
# ─────────────────────────────────────────────────────────────────────────────
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

env:
  GCC_VERSION: "15"
  CLANG_VERSION: "22"

# ─────────────────────────────────────────────────────────────────────────────
# Stage 1: Plain tests (no sanitizers)
# ─────────────────────────────────────────────────────────────────────────────
jobs:
  test-linux:
    name: "Test · ${{ matrix.preset }}"
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - preset: linux-gcc
            configure: linux-gcc-debug
            build: linux-gcc-debug
            test: linux-gcc-test
            compiler: gcc
          - preset: linux-clang
            configure: linux-clang-debug
            build: linux-clang-debug
            test: linux-clang-test
            compiler: clang
    steps:
      - uses: actions/checkout@v4
      - name: Install compiler toolchain
        run: |
          sudo apt-get update
          sudo apt-get install -y ninja-build wget gnupg lsb-release software-properties-common

          if [ "${{ matrix.compiler }}" = "gcc" ]; then
            bash .github/workflows/install_gcc.sh "${{ env.GCC_VERSION }}"
          else
            bash .github/workflows/install_llvm.sh "${{ env.CLANG_VERSION }}"
          fi
      - name: Install Conan
        run: |
          pip install conan
          conan profile detect
      - name: Configure
        run: cmake --preset ${{ matrix.configure }}
      - name: Build
        run: cmake --build --preset ${{ matrix.build }}
      - name: Test
        run: ctest --preset ${{ matrix.test }}

  test-linux-coverage:
    name: "Coverage · linux-gcc"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install system packages
        run: |
          sudo apt-get update
          sudo apt-get install -y ninja-build lcov wget gnupg lsb-release software-properties-common
          bash .github/workflows/install_gcc.sh "${{ env.GCC_VERSION }}"
      - name: Install Conan
        run: |
          pip install conan
          conan profile detect
      - name: Configure
        run: cmake --preset linux-gcc-debug -DENABLE_COVERAGE=ON
      - name: Build
        run: cmake --build --preset linux-gcc-debug
      - name: Reset coverage counters
        run: find build/linux-gcc-debug -name '*.gcda' -delete
      - name: Test
        run: ctest --preset linux-gcc-test
      - name: Generate coverage HTML report
        run: |
          COVERAGE_DIR=build/linux-gcc-debug/coverage
          ROOT_DIR=$(pwd)
          RAW_COVERAGE_INFO="$COVERAGE_DIR/coverage.raw.info"
          FILTERED_COVERAGE_INFO="$COVERAGE_DIR/coverage.info"
          GCC_MAJOR=$(gcc -dumpfullversion | cut -d. -f1)
          GCOV_TOOL=$(command -v "gcov-${GCC_MAJOR}" || command -v gcov)

          mkdir -p "$COVERAGE_DIR"

          echo "Using gcov tool: $GCOV_TOOL"
          "$GCOV_TOOL" --version | head -n 1

          echo "Capturing coverage data with lcov..."
          lcov --capture --directory build/linux-gcc-debug --gcov-tool "$GCOV_TOOL" --ignore-errors mismatch --output-file "$RAW_COVERAGE_INFO"
          
          echo "Filtering coverage data to include only project source files..."
          lcov --ignore-errors unused --extract "$RAW_COVERAGE_INFO" "$ROOT_DIR/src/*" "$ROOT_DIR/include/*" "$ROOT_DIR/apps/*" --output-file "$FILTERED_COVERAGE_INFO"

          echo "Generating HTML report with genhtml..."
          genhtml "$FILTERED_COVERAGE_INFO" --output-directory "$COVERAGE_DIR/html"
      - name: Upload coverage HTML artifact
        uses: actions/upload-artifact@v4
        with:
          name: coverage-html-linux-gcc-${{ github.run_number }}
          path: build/linux-gcc-debug/coverage/html
          if-no-files-found: error
          retention-days: 14

  docs-linux:
    name: "Docs · linux-gcc-debug"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install system packages
        run: |
          sudo apt-get update
          sudo apt-get install -y ninja-build doxygen wget gnupg lsb-release software-properties-common
          bash .github/workflows/install_gcc.sh "${{ env.GCC_VERSION }}"
      - name: Install Conan
        run: |
          pip install conan
          conan profile detect
      - name: Configure
        run: cmake --preset linux-gcc-debug
      - name: Build Doxygen docs
        run: cmake --build --preset linux-gcc-debug --target docs
      - name: Upload Doxygen HTML artifact
        uses: actions/upload-artifact@v4
        with:
          name: doxygen-html-${{ github.run_number }}
          path: build/linux-gcc-debug/docs/html
          if-no-files-found: error
          retention-days: 14

  test-macos:
    name: "Test · ${{ matrix.preset }}"
    runs-on: macos-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - preset: macos-gcc
            configure: macos-gcc-debug
            build: macos-gcc-debug
            test: macos-gcc-test
            brew_packages: gcc ninja cmake conan
          - preset: macos-clang
            configure: macos-clang-debug
            build: macos-clang-debug
            test: macos-clang-test
            brew_packages: llvm ninja cmake conan
    steps:
      - uses: actions/checkout@v4
      - name: Select Xcode (runner default)
        run: |
          sudo xcode-select -s "$(xcode-select -p)"
          xcodebuild -version
      - name: Install Homebrew packages
        run: brew install ${{ matrix.brew_packages }}
      - name: Detect Conan
        run: |
          conan profile detect
      - name: Configure
        run: cmake --preset ${{ matrix.configure }} -DCMAKE_OSX_SYSROOT=$(xcrun --show-sdk-path)
      - name: Build
        run: cmake --build --preset ${{ matrix.build }}
      - name: Test
        run: ctest --preset ${{ matrix.test }}

  test-windows-msys2:
    name: "Test · ${{ matrix.preset }}"
    runs-on: windows-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - preset: win-msys2-gcc
            configure: win-msys2-gcc-debug
            build: win-msys2-gcc-debug
            test: win-msys2-gcc-test
            msystem: MINGW64
            msys_packages: mingw-w64-x86_64-gcc mingw-w64-x86_64-ninja mingw-w64-x86_64-cmake mingw-w64-x86_64-python-pip
          - preset: win-msys2-clang-libstdc++
            configure: win-msys2-clang-libstdc++-debug
            build: win-msys2-clang-libstdc++-debug
            test: win-msys2-clang-libstdc++-test
            msystem: MINGW64
            msys_packages: mingw-w64-x86_64-clang mingw-w64-x86_64-ninja mingw-w64-x86_64-cmake mingw-w64-x86_64-python-pip
          - preset: win-msys2-clang
            configure: win-msys2-clang-debug
            build: win-msys2-clang-debug
            test: win-msys2-clang-test
            msystem: CLANG64
            msys_packages: mingw-w64-clang-x86_64-clang mingw-w64-clang-x86_64-ninja mingw-w64-clang-x86_64-cmake mingw-w64-clang-x86_64-python-pip mingw-w64-clang-x86_64-toolchain
    defaults:
      run:
        shell: msys2 {0}
    steps:
      - uses: actions/checkout@v4
      - uses: msys2/setup-msys2@v2
        with:
          msystem: ${{ matrix.msystem }}
          install: ${{ matrix.msys_packages }}
          update: true
      - name: Install Conan
        run: |
          pip install conan
          conan profile detect
      - name: Configure
        run: cmake --preset ${{ matrix.configure }}
      - name: Build
        run: cmake --build --preset ${{ matrix.build }}
      - name: Test
        run: ctest --preset ${{ matrix.test }}

  test-windows-msvc:
    name: "Test · win-msvc"
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install Ninja
        run: choco install ninja -y
      - name: Install Conan
        run: |
          pip install conan
          conan profile detect
      - name: Configure (Developer Command Prompt)
        uses: ilammy/msvc-dev-cmd@v1
      - name: Configure
        run: cmake --preset win-msvc-debug
      - name: Build
        run: cmake --build --preset win-msvc-debug
      - name: Test
        run: ctest --preset win-msvc-test

  # ───────────────────────────────────────────────────────────────────────────
  # Stage 2: Valgrind memcheck (Linux only, no sanitizers)
  # ───────────────────────────────────────────────────────────────────────────
  valgrind:
    name: "Valgrind · ${{ matrix.preset }}"
    needs: test-linux
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - preset: linux-gcc
            configure: linux-gcc-debug
            build: linux-gcc-debug
            compiler: gcc
          - preset: linux-clang
            configure: linux-clang-debug
            build: linux-clang-debug
            compiler: clang
    steps:
      - uses: actions/checkout@v4
      - name: Install system packages and compiler toolchain
        run: |
          sudo apt-get update
          sudo apt-get install -y ninja-build valgrind wget gnupg lsb-release software-properties-common

          if [ "${{ matrix.compiler }}" = "gcc" ]; then
            bash .github/workflows/install_gcc.sh "${{ env.GCC_VERSION }}"
          else
            bash .github/workflows/install_llvm.sh "${{ env.CLANG_VERSION }}"
          fi
      - name: Install Conan
        run: |
          pip install conan
          conan profile detect
      - name: Configure
        run: cmake --preset ${{ matrix.configure }}
      - name: Build
        run: cmake --build --preset ${{ matrix.build }}
      - name: Valgrind memcheck
        run: ctest --test-dir build/${{ matrix.configure }} -T memcheck --output-on-failure

  # ───────────────────────────────────────────────────────────────────────────
  # Stage 3: Leaks memcheck (MacOS only, no sanitizers)
  # ───────────────────────────────────────────────────────────────────────────
  leaks-macos:
    name: "Leaks · ${{ matrix.preset }}"
    needs: test-macos
    runs-on: macos-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - preset: macos-gcc
            configure: macos-gcc-debug
            build: macos-gcc-debug
            brew_packages: gcc ninja cmake conan
          - preset: macos-clang
            configure: macos-clang-debug
            build: macos-clang-debug
            brew_packages: llvm ninja cmake conan
    steps:
      - uses: actions/checkout@v4
      - name: Select Xcode (runner default)
        run: |
          sudo xcode-select -s "$(xcode-select -p)"
          xcodebuild -version
      - name: Install Homebrew packages
        run: brew install ${{ matrix.brew_packages }}
      - name: Install Conan
        run: |
          conan profile detect
      - name: Configure
        run: cmake --preset ${{ matrix.configure }} -DCMAKE_OSX_SYSROOT=$(xcrun --show-sdk-path)
      - name: Build
        run: cmake --build --preset ${{ matrix.build }}
      - name: Leaks check
        run: |
          TEST_EXE_NAME=$(python3 .github/workflows/resolve_test_executable.py)
          echo "Resolved test executable: $TEST_EXE_NAME"
          MallocStackLogging=1 leaks --atExit -- "./build/${{ matrix.configure }}/test/${TEST_EXE_NAME}"

  # ───────────────────────────────────────────────────────────────────────────
  # Stage 4: Sanitizer tests
  # ───────────────────────────────────────────────────────────────────────────
  sanitizers-linux:
    name: "Sanitizer · ${{ matrix.test }}"
    needs: test-linux
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          # GCC sanitizers
          - configure: linux-gcc-debug-asan
            build: linux-gcc-debug-asan
            test: linux-gcc-test-asan
            compiler: gcc
          - configure: linux-gcc-debug-tsan
            build: linux-gcc-debug-tsan
            test: linux-gcc-test-tsan
            compiler: gcc
          - configure: linux-gcc-debug-lsan
            build: linux-gcc-debug-lsan
            test: linux-gcc-test-lsan
            compiler: gcc
          # Clang sanitizers
          - configure: linux-clang-debug-asan
            build: linux-clang-debug-asan
            test: linux-clang-test-asan
            compiler: clang
          - configure: linux-clang-debug-tsan
            build: linux-clang-debug-tsan
            test: linux-clang-test-tsan
            compiler: clang
          - configure: linux-clang-debug-lsan
            build: linux-clang-debug-lsan
            test: linux-clang-test-lsan
            compiler: clang
    steps:
      - uses: actions/checkout@v4
      - name: Install compiler toolchain
        run: |
          sudo apt-get update
          sudo apt-get install -y ninja-build wget gnupg lsb-release software-properties-common

          if [ "${{ matrix.compiler }}" = "gcc" ]; then
            bash .github/workflows/install_gcc.sh "${{ env.GCC_VERSION }}"
          else
            bash .github/workflows/install_llvm.sh "${{ env.CLANG_VERSION }}"
          fi
      - name: Install Conan
        run: |
          pip install conan
          conan profile detect
      - name: Configure
        run: cmake --preset ${{ matrix.configure }}
      - name: Build
        run: cmake --build --preset ${{ matrix.build }}
      - name: Test
        run: ctest --preset ${{ matrix.test }}


  sanitizers-macos:
    name: "Sanitizer · ${{ matrix.test }}"
    needs: test-macos
    runs-on: macos-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - configure: macos-clang-debug-asan
            build: macos-clang-debug-asan
            test: macos-clang-test-asan
            brew_packages: llvm ninja cmake conan
          - configure: macos-clang-debug-tsan
            build: macos-clang-debug-tsan
            test: macos-clang-test-tsan
            brew_packages: llvm ninja cmake conan
    steps:
      - uses: actions/checkout@v4
      - name: Select Xcode (runner default)
        run: |
          sudo xcode-select -s "$(xcode-select -p)"
          xcodebuild -version
      - name: Install Homebrew packages
        run: brew install ${{ matrix.brew_packages }}
      - name: Install Conan
        run: |
          conan profile detect
      - name: Configure
        run: cmake --preset ${{ matrix.configure }} -DCMAKE_OSX_SYSROOT=$(xcrun --show-sdk-path)
      - name: Build
        run: cmake --build --preset ${{ matrix.build }}
      - name: Test
        run: ctest --preset ${{ matrix.test }}

  sanitizers-windows-msys2:
    name: "Sanitizer · ${{ matrix.test }}"
    needs: test-windows-msys2
    runs-on: windows-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - configure: win-msys2-clang-debug-asan
            build: win-msys2-clang-debug-asan
            test: win-msys2-clang-test-asan
            msystem: CLANG64
            msys_packages: mingw-w64-clang-x86_64-clang mingw-w64-clang-x86_64-ninja mingw-w64-clang-x86_64-cmake mingw-w64-clang-x86_64-python-pip mingw-w64-clang-x86_64-toolchain
    defaults:
      run:
        shell: msys2 {0}
    steps:
      - uses: actions/checkout@v4
      - uses: msys2/setup-msys2@v2
        with:
          msystem: ${{ matrix.msystem }}
          install: ${{ matrix.msys_packages }}
          update: true
      - name: Install Conan
        run: |
          pip install conan
          conan profile detect
      - name: Configure
        run: cmake --preset ${{ matrix.configure }}
      - name: Build
        run: cmake --build --preset ${{ matrix.build }}
      - name: Test
        run: ctest --preset ${{ matrix.test }}

  sanitizers-windows-msvc:
    name: "Sanitizer · win-msvc-test-asan"
    needs: test-windows-msvc
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install Ninja
        run: choco install ninja -y
      - name: Install Conan
        run: |
          pip install conan
          conan profile detect
      - uses: ilammy/msvc-dev-cmd@v1
      - name: Configure
        run: cmake --preset win-msvc-debug-asan
      - name: Build
        run: cmake --build --preset win-msvc-debug-asan
      - name: Test
        run: ctest --preset win-msvc-test-asan

  # ───────────────────────────────────────────────────────────────────────────
  # Stage 5: Static analysis (Linux only)
  # ───────────────────────────────────────────────────────────────────────────
  static-analysis-cppcheck-linux:
    name: "Static Analysis · cppcheck"
    needs: test-linux
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install system packages
        run: |
          sudo apt-get update
          sudo apt-get install -y ninja-build cppcheck wget gnupg lsb-release software-properties-common
          bash .github/workflows/install_gcc.sh "${{ env.GCC_VERSION }}"
      - name: Install Conan
        run: |
          pip install conan
          conan profile detect
      - name: Configure
        run: cmake --preset linux-gcc-debug
      - name: Build
        run: cmake --build --preset linux-gcc-debug
      - name: Run cppcheck
        run: |
          mkdir -p build/linux-gcc-debug/static-analysis

          # Machine-readable report
          cppcheck \
            --project=build/linux-gcc-debug/compile_commands.json \
            --enable=all \
            --inconclusive \
            --suppress=missingIncludeSystem \
            --suppress=checkersReport \
            --xml \
            --xml-version=2 \
            --output-file=build/linux-gcc-debug/static-analysis/cppcheck.xml \
            2> build/linux-gcc-debug/static-analysis/cppcheck.stderr.txt

          # Human-readable report
          cppcheck \
            --project=build/linux-gcc-debug/compile_commands.json \
            --enable=all \
            --inconclusive \
            --suppress=missingIncludeSystem \
            --suppress=checkersReport \
            --template=gcc \
            2> build/linux-gcc-debug/static-analysis/cppcheck.txt

      - name: Print cppcheck reports to job output
        run: |
          echo "--- cppcheck human-readable report ---"
          if [ -f build/linux-gcc-debug/static-analysis/cppcheck.txt ]; then
            cat build/linux-gcc-debug/static-analysis/cppcheck.txt
          else
            echo "cppcheck.txt not found"
          fi

          echo "--- cppcheck XML run diagnostics (stderr) ---"
          if [ -f build/linux-gcc-debug/static-analysis/cppcheck.stderr.txt ]; then
            cat build/linux-gcc-debug/static-analysis/cppcheck.stderr.txt
          else
            echo "cppcheck.stderr.txt not found"
          fi

      - name: Upload cppcheck artifacts
        uses: actions/upload-artifact@v4
        with:
          name: static-analysis-cppcheck-${{ github.run_number }}
          path: |
            build/linux-gcc-debug/static-analysis/cppcheck.xml
            build/linux-gcc-debug/static-analysis/cppcheck.stderr.txt
            build/linux-gcc-debug/static-analysis/cppcheck.txt
          if-no-files-found: error
          retention-days: 14

  static-analysis-pvs-linux:
    name: "Static Analysis · PVS-Studio"
    needs: test-linux
    runs-on: ubuntu-latest
    env:
      PVS_STUDIO_LICENSE: ${{ secrets.PVS_STUDIO_LICENSE }}
    steps:
      - uses: actions/checkout@v4
      - name: Prepare PVS output directory
        run: |
          mkdir -p build/linux-gcc-debug/static-analysis

      - name: Check PVS license
        id: pvs-license
        run: |
          if [ -z "$PVS_STUDIO_LICENSE" ]; then
            echo "PVS_STUDIO_LICENSE is not set; skipping PVS-Studio." > build/linux-gcc-debug/static-analysis/pvs-skipped.txt
            echo "skip=true" >> "$GITHUB_OUTPUT"
          else
            echo "skip=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Install system packages
        if: steps.pvs-license.outputs.skip != 'true'
        run: |
          sudo apt-get update
          sudo apt-get install -y ninja-build wget gnupg lsb-release software-properties-common
          bash .github/workflows/install_gcc.sh "${{ env.GCC_VERSION }}"

      - name: Install Conan
        if: steps.pvs-license.outputs.skip != 'true'
        run: |
          pip install conan
          conan profile detect

      - name: Install PVS-Studio
        if: steps.pvs-license.outputs.skip != 'true'
        run: |
          wget -q -O - https://files.pvs-studio.com/etc/pubkey.txt | sudo apt-key add -
          sudo wget -q -O /etc/apt/sources.list.d/viva64.list https://files.pvs-studio.com/etc/viva64.list
          sudo apt-get update
          sudo apt-get install -y pvs-studio

      - name: Configure PVS-Studio license
        if: steps.pvs-license.outputs.skip != 'true'
        run: |
          mkdir -p "$HOME/.config/PVS-Studio"
          printf '%s\n' "$PVS_STUDIO_LICENSE" > "$HOME/.config/PVS-Studio/PVS-Studio.lic"
          chmod 600 "$HOME/.config/PVS-Studio/PVS-Studio.lic"

      - name: Configure CMake
        if: steps.pvs-license.outputs.skip != 'true'
        run: |
          # Configure after PVS installation so CMake can add the pvs_analysis target.
          cmake --preset linux-gcc-debug

      - name: Build
        if: steps.pvs-license.outputs.skip != 'true'
        run: |
          cmake --build --preset linux-gcc-debug

      - name: Run pvs_analysis target
        if: steps.pvs-license.outputs.skip != 'true'
        run: |
          cmake --build --preset linux-gcc-debug --target pvs_analysis

      - name: Convert PVS report
        if: steps.pvs-license.outputs.skip != 'true'
        run: |
          if [ -f build/linux-gcc-debug/pvs_report.log ]; then
            plog-converter -t tasklist -o build/linux-gcc-debug/static-analysis/pvs_report.tasklist.txt build/linux-gcc-debug/pvs_report.log || true
            plog-converter -t fullhtml -o build/linux-gcc-debug/static-analysis/pvs_html build/linux-gcc-debug/pvs_report.log || true
          fi

      - name: Print PVS reports to job output
        run: |
          if [ -f build/linux-gcc-debug/static-analysis/pvs-skipped.txt ]; then
            echo "--- PVS skipped ---"
            cat build/linux-gcc-debug/static-analysis/pvs-skipped.txt
          fi

          echo "--- PVS raw report log ---"
          if [ -f build/linux-gcc-debug/pvs_report.log ]; then
            cat build/linux-gcc-debug/pvs_report.log
          else
            echo "pvs_report.log not found"
          fi
          
          echo "--- PVS report json ---"
          if [ -f build/linux-gcc-debug/pvs_report.json ]; then
            cat build/linux-gcc-debug/pvs_report.json
          else
            echo "pvs_report.json not found"
          fi

          echo "--- PVS tasklist report ---"
          if [ -f build/linux-gcc-debug/static-analysis/pvs_report.tasklist.txt ]; then
            cat build/linux-gcc-debug/static-analysis/pvs_report.tasklist.txt
          else
            echo "pvs_report.tasklist.txt not found"
          fi

      - name: Upload PVS artifacts
        uses: actions/upload-artifact@v4
        with:
          name: static-analysis-pvs-${{ github.run_number }}
          path: |
            build/linux-gcc-debug/static-analysis/pvs-skipped.txt
            build/linux-gcc-debug/pvs_report.log
            build/linux-gcc-debug/pvs_report.json
            build/linux-gcc-debug/static-analysis/pvs_report.tasklist.txt
            build/linux-gcc-debug/static-analysis/pvs_html
          if-no-files-found: ignore
          retention-days: 14

  static-analysis-sonar-linux:
    name: "Static Analysis · SonarCloud"
    needs: test-linux
    runs-on: ubuntu-latest
    env:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
      BUILD_WRAPPER_OUT_DIR: build/linux-gcc-debug/static-analysis/sonar/build-wrapper
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Prepare Sonar build prerequisites
        run: |
          mkdir -p build/linux-gcc-debug/static-analysis/sonar

          if [ -z "$SONAR_TOKEN" ]; then
            echo "SONAR_TOKEN is not set; skipping Sonar." > build/linux-gcc-debug/static-analysis/sonar/sonar-skipped.txt
            exit 0
          fi

          sudo apt-get update
          sudo apt-get install -y ninja-build wget gnupg lsb-release software-properties-common

          bash .github/workflows/install_gcc.sh "${{ env.GCC_VERSION }}"

          pip install conan
          conan profile detect

          cmake --preset linux-gcc-debug

      - name: Install Build Wrapper
        uses: SonarSource/sonarqube-scan-action/install-build-wrapper@v6

      - name: Run Build Wrapper
        run: |
          build-wrapper-linux-x86-64 --out-dir "${{ env.BUILD_WRAPPER_OUT_DIR }}" \
            cmake --build --preset linux-gcc-debug --target all

          # Keep a compile_commands copy next to build-wrapper data for easier debugging.
          cp build/linux-gcc-debug/compile_commands.json "${{ env.BUILD_WRAPPER_OUT_DIR }}/compile_commands.json"

      - name: SonarQube Cloud Scan
        uses: SonarSource/sonarqube-scan-action@v6
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        with:
          # CFamily now consumes the compilation database generated by build-wrapper.
          args: >
            --define sonar.cfamily.compile-commands=${{ env.BUILD_WRAPPER_OUT_DIR }}/compile_commands.json

      - name: Build human-readable Sonar report
        run: |
          mkdir -p build/linux-gcc-debug/static-analysis/sonar
          SONAR_REPORT=build/linux-gcc-debug/static-analysis/sonar/sonar-summary.txt

          if [ -f build/linux-gcc-debug/static-analysis/sonar/sonar-skipped.txt ]; then
            cat build/linux-gcc-debug/static-analysis/sonar/sonar-skipped.txt > "$SONAR_REPORT"
            exit 0
          fi

          if [ -f .scannerwork/report-task.txt ]; then
            cat .scannerwork/report-task.txt >> "$SONAR_REPORT"
          else
            echo "report-task.txt not found; check Sonar action logs." >> "$SONAR_REPORT"
          fi

      - name: Print Sonar summary to job output
        run: |
          SONAR_REPORT=build/linux-gcc-debug/static-analysis/sonar/sonar-summary.txt
          echo "--- Sonar summary artifact content ---"
          cat "$SONAR_REPORT"

          {
            echo "## SonarCloud summary"
            echo "- Artifact: static-analysis-sonar-${{ github.run_number }}"
            echo "- File: build/linux-gcc-debug/static-analysis/sonar/sonar-summary.txt"
            echo ""
            echo "\`report-task.txt\` includes the Sonar dashboard URL and CE task URL."
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Upload Sonar artifacts
        uses: actions/upload-artifact@v4
        with:
          name: static-analysis-sonar-${{ github.run_number }}
          path: |
            build/linux-gcc-debug/static-analysis/sonar
            .scannerwork/report-task.txt
          if-no-files-found: ignore
          retention-days: 14
